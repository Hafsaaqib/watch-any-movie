apiVersion: apps/v1
kind: Deployment
metadata:
  name: aks-deployment
  labels:
    app: aks-deployment
  namespace: namespace-workflow-1702397773127
spec:
  replicas: 1
  selector:
    matchLabels:
      app: aks-deployment
  template:
    metadata:
      labels:
        app: aks-deployment
    spec:
      containers:
        - name: aks-deployment
          image: myacr00123.azurecr.io/image-workflow-1702397773127:latest
          ports:
            - containerPort: 3000